# smart-traffic-route-optimier-dsa
Smart Traffic Route Optimizer is a desktop application built using Java Swing and a C++ algorithm engine integrated via JNI. It uses Graphs and Dijkstra’s algorithm for route optimization, AVL Trees for traffic congestion management, and Hash Tables for fast location lookup, providing dynamic, traffic-aware routing.
